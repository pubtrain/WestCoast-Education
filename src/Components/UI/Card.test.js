import { render, screen } from '@testing-library/react';

describe('', () => {
  const setup = () => render();

  describe('', () => {
    test('', () => {
      setup();
    });
  });
});

// Detta test lyckades jag inte köra då det finns för lite innehåll i mitt kort
