import { render, screen } from '@testing-library/react';

describe('', () => {
  const setup = () => render();

  describe('', () => {
    test('', () => {
      setup();
    });
  });
});

// Fick meddelandet "Target container is not a DOM element.". Kan inte köra testet, vi har inte lärt oss testa portals.
